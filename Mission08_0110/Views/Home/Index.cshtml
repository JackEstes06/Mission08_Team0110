@{
    ViewData["Title"] = "Home Page";
}
@{
    Layout = "_Layout";
}

<div class="d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="text-center">
        <!-- Header content is handled by the shared layout, no need to include it here -->
        <h1 class="mb-4">What are you gonna do today?</h1>

        <!-- Buttons -->
        <div class="d-flex justify-content-center gap-3">
            <a asp-controller="AddTask"  class="btn btn-primary btn-lg">Add Task</a>
            <a href="/Quadrants" class="btn btn-secondary btn-lg">View Tasks</a>
        </div>
    </div>
</div>

